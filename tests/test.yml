---
- hosts: travis
  connection: docker
  remote_user: root
  vars:
    mods_enabled:
      - userdir
  roles:
    - {
      role: ansible-role-apache,
      apache_mods_enabled: '{{ mods_enabled }}',
      apache_init_system: 'runit'
    }